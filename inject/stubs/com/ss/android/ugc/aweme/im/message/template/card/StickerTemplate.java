package com.ss.android.ugc.aweme.im.message.template.card;

/**
 * A sticker as a message carries it.
 *
 * Only the name matters here. What is inside is read by walking the object at
 * runtime rather than by naming its parts: the parts are obfuscated and the
 * walk costs nothing on a tap.
 */
public class StickerTemplate {
}
